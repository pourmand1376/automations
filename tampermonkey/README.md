# Tampermonkey scripts

Install `motamem-raindrop-checkmarks.user.js` in Tampermonkey. On Motamem pages, it downloads the JSON URL list from GitHub, caches it for four hours, and adds a green checkmark after every matching link. It also checks links added dynamically after the initial page load.
